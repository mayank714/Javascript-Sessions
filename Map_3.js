let  arr =[2,3,4,6,8,10];
let res_Arr = arr.map((v)=>{
    if(v %2==0){
        return v;
    }
    return v;
})
console.log(`Size of OArr is ${arr.length}. Size of res_arr is ${res_Arr.length}`)
console.log(res_Arr)